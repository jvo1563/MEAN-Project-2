import { HeaderAdderInterceptor } from './header-adder.interceptor';

describe('HeaderAdderInterceptor', () => {
  it('should be defined', () => {
    expect(new HeaderAdderInterceptor()).toBeDefined();
  });
});
